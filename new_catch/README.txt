The version of the game in `index.html` runs.

The jspsych plugin version in `jspsych-catch-game.js` called by `catch-demo.html` does not display (though the game seems to be running because it still generates score update boxes).

